version: 2
jobs:
  BuildApplication:
    working_directory: node-express-circleci-test/project
    docker:
      - image: 'circleci/node:8.10.0'
    steps:
      - checkout
      - restore_cache:
          key: 'v2-dependencies-{{ checksum "package.json" }}'
      - run:
          name: Install dependencies
          command: npm install
      - save_cache:
          key: 'v2-dependencies-{{ checksum "package.json" }}'
          paths:
              - node_modules
  LintCode:
    working_directory: node-express-circleci-test/project
    docker:
      - image: 'circleci/node:8.10.0'
    steps:
      - checkout
      - restore_cache:
          key: 'v2-dependencies-{{ checksum "package.json" }}'
      - run:
          name: Lint code
          command: npm run lint
  MochaTests:
    working_directory: node-express-circleci-test/project
    docker:
      - image: 'circleci/node:8.10.0'
    steps:
      - checkout
      - restore_cache:
          key: 'v2-dependencies-{{ checksum "package.json" }}'
      - run:
          name: Test code
          command: npm test
workflows:
  version: 2
  BuildApplication-LintCode-MochaTests:
    jobs:
      - BuildApplication
      - LintCode:
          requires: 
            - BuildApplication
      - MochaTests:
          requires: 
            - BuildApplication
          context: app-environment-variables
